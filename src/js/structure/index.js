const queue = require('./queue');
const stack = require('./stack');
const dll = require('./dll');

(function () {
  'use strict';

  exports.queue = queue;
  exports.stack = stack;
  exports.dll = dll;
})();
